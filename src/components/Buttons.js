import React from 'react'
import "./Buttons.css"

const Buttons = ({ symbol }) => {
    return (
        <div className="button-main">
            {symbol}
        </div>
    )
}

export default Buttons
